# Browser-facing API base path
VITE_API_URL=/api

# Vite dev server proxy target for non-Docker local development
API_PROXY_TARGET=http://localhost:4000

# Comma-separated hostnames the Vite dev server should answer for
ALLOWED_HOSTS=semesti.plani.dev
